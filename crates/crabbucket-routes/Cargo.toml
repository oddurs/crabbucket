# Copyright (C) 2026 Oddur Sigurdsson
# SPDX-License-Identifier: GPL-3.0-or-later

[package]
name = "crabbucket-routes"
description = "Derives a site's routes from its content directory, and generates a Route enum."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true

# No dependencies, deliberately.  This is compiled during a site's build phase,
# before anything else, and a Markdown parser has no business being there.
